### AI Usage

- Used to generate the grids on the initial sign in page
- Used to generate loading skeleton components
- Used to generate the instruction sent to OpenAI
- I've been getting used to calling backend services from separate API routes set up through Express servers so I used Claude as a refresher for the conventions of server actions and route handling.
- Used to help setup test structure
- Used to help structure and format README
